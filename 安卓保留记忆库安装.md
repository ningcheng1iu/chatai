# 安卓保留记忆库安装

这个项目的聊天记忆库存放在 Android WebView 的 `localStorage` 里。只要是同一个包名、同一个签名的覆盖安装，Android 会保留应用数据，记忆库就不会丢。

## 推荐流程

修改前端文件后运行：

```bash
npm run android:sync
npm run android:install:keep
```

`android:install:keep` 会执行 Gradle 的 `installDebug`，相当于覆盖安装，不会主动卸载旧版本。

如果已经先打好 APK，也可以手动覆盖安装：

```bash
adb install -r android/app/build/outputs/apk/debug/app-debug.apk
```

如果手机上装的版本号比当前 APK 高，覆盖安装可能失败。这种情况可以先提高 `android/app/build.gradle` 里的 `versionCode`，或者在确认要降级覆盖时运行：

```bash
npm run android:install:keep:downgrade
```

## 会导致记忆库丢失的情况

- 先卸载旧 App，再安装新 APK。
- 在系统设置里点了“清除存储”或“清除数据”。
- 使用了 `adb uninstall 包名`。
- 包名变了，比如 `com.example.deepseekchat` 改成了别的。
- 签名变了，并且系统要求先卸载旧版本才能安装。

当前包名在这两个文件里保持一致：

- `capacitor.config.json` 的 `appId`
- `android/app/build.gradle` 的 `applicationId`

## 更稳的长期方案

覆盖安装只能保证“不卸载时”保留数据。如果以后希望卸载重装也能恢复记忆库，需要额外做“导出/导入记忆库”或云端同步。
